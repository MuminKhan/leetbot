{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "SAM build/deploy application",
            "type": "shell",
            "command": "sam build && sam deploy --no-confirm-changeset",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Ship it!"
        },
    ]
}